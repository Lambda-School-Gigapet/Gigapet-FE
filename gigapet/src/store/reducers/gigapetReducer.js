const initialState = {
    level: 0,
    points: 0,
    mood: 'neutral'
}

export default function gigapetReducer(state=initialState, action) {
    switch(action.type) {
        default: 
            return state
    }
}